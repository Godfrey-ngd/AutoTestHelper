[pytest]
testpaths = target-app-tests autotestdesign/tests
pythonpath = .
addopts = -v --tb=short
# Used by pytest-playwright / pytest-base-url (session scope; do not override in conftest)
base_url = http://127.0.0.1:5000
